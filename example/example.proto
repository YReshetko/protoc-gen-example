syntax = "proto3";

import "proto/ext.proto";

package test.example;

message Request {
    // request body
    string msg = 1;
}
message Response {
    // response body
    string msg = 1;
}


service Service {
    rpc SomeMethod (Request) returns (Response) {
        option (roles) = "Admin,User";
    }
    rpc SomeAnotherMethod (Request) returns (Response) {
        option (roles) = "SuperAdmin";
    }
    rpc SomeDifferentMethod (Request) returns (Response) {
        option (roles) = "User,Unknown";
    }
}
